module.exports = {
    testEnvironment: "node", // or "jsdom" if you test browser code
    testMatch: ["**/tests/**/*.test.js", "**/?(*.)+(spec|test).js"], // where your test files are
    verbose: true, // optional: show detailed test results
};
